Quinn CRM — Frontend README

Overview
- React + Vite + TypeScript + Tailwind CSS + CSS modules.

Run locally
1. cd frontend
2. npm install
3. npm run dev

Environment
- Provide firebase config in frontend/.env (placeholder provided). Do not commit production keys.
